/**
 * Mapper futuro para Reportes.
 *
 * Reportes suele recibir respuestas mas complejas que una tabla simple.
 * Aqui se normalizaria lo que venga del backend para convertirlo en:
 *
 * - summary;
 * - daily;
 * - channels;
 * - products.
 *
 * Por ahora `data/reportsData.ts` ya esta en formato UI.
 */
export {};
