import { EndpointPendingView } from "@/shared/components/EndpointPendingView";

const Reports = () => (
  <EndpointPendingView
    title="Reportes"
    description="La vista queda preparada para reportes reales. Las graficas y tablas se conectaran solo cuando backend entregue endpoints oficiales."
  />
);

export default Reports;
