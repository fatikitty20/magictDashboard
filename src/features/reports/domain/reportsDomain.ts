/**
 * Capa de dominio futura para Reportes.
 *
 * Aqui vivirian reglas como:
 *
 * - seleccionar rango de fechas;
 * - elegir metrica activa;
 * - calcular resumenes;
 * - filtrar productos por canal;
 * - transformar reportes por rol si backend aun no lo entrega separado.
 *
 * Por ahora parte de esta logica sigue en `views/Reports.tsx` por ser mock.
 */
export {};
