/**
 * Capa API futura para Pedidos.
 *
 * Hoy Pedidos usa datos mock desde `services/ordersService.ts`.
 * Cuando Alejandro exponga endpoints de pedidos, aqui se deberian construir las
 * llamadas HTTP, por ejemplo:
 *
 * - GET /api/v1/orders?page=1&limit=20
 * - GET /api/v1/orders/:id
 * - GET /api/v1/orders?status=pending
 *
 * Esta capa debe depender de `src/shared/api/apiClient.ts`, no de `fetch` directo.
 */
export {};
