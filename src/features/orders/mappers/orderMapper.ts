/**
 * Mapper futuro para Pedidos.
 *
 * Aqui se traduciria el DTO del backend al tipo `Order` usado por la UI.
 * Esto evita que la tabla dependa de nombres de columnas de base de datos o de
 * formatos propios del backend.
 *
 * Por ahora los datos mock ya llegan con la forma correcta.
 */
export {};
