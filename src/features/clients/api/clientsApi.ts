/**
 * Capa API futura para Clientes.
 *
 * Hoy la vista de clientes usa datos mock desde `services/clientsService.ts`.
 * Cuando el backend tenga endpoints reales, aqui deberian vivir las llamadas HTTP
 * relacionadas con clientes, por ejemplo:
 *
 * - GET /api/v1/clients?page=1&limit=20
 * - GET /api/v1/clients/:id
 * - GET /api/v1/clients?status=active&tier=gold
 *
 * Esta capa debe usar `src/shared/api/apiClient.ts`, igual que Payments.
 */
export {};
