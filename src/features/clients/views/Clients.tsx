import { EndpointPendingView } from "@/shared/components/EndpointPendingView";

const Clients = () => (
  <EndpointPendingView
    title="Clientes"
    description="La carpeta se conserva para la vista de clientes, pero no usa datos simulados ni reglas inventadas de rol."
  />
);

export default Clients;
