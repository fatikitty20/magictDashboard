/**
 * Mapper futuro para Clientes.
 *
 * Un mapper traduce la respuesta cruda del backend al modelo que entiende la UI.
 * Ejemplo: si backend responde `created_at`, la UI puede recibir `createdAt`.
 *
 * Por ahora no hace falta porque `data/clientsData.ts` ya tiene la forma que usa
 * `types/client.ts`.
 */
export {};
